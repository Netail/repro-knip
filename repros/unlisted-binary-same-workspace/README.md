# unused-files-next-subdirectory

```sh
Unlisted binaries (1)
test-cli                  repros/unlisted-binary-same-workspace/package.json
```

`test-cli` is marked as unlisted binary, while the binary is defined in the same package.json

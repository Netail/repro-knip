# unlisted-binary-npx

```sh
Unlisted binaries (1)
ts-json-schema-generator  repros/unlisted-binary-npx/package.json
```

`ts-json-schema-generator` is being loaded through npx and isn't installed like a regular dependency
